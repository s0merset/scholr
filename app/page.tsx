import { LandingPage } from "@/components/landing/LandingPage";

export default function Home() {
  // page.tsx is now just a wrapper that renders the parent landing page
  return <LandingPage />;
}

